server:
  port: 8080
spring:
  application:
    name: devcontainers-demo
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:books}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: update
